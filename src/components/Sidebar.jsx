import React from 'react';

// Sidebar intentionally removed: BlueFox now uses the full browser window.
const Sidebar = () => null;
const SidebarPanel = () => null;

export { Sidebar, SidebarPanel };
export default Sidebar;
